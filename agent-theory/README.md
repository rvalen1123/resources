# Agent Theory

Conceptual notes on agent architecture: ReAct, multi-agent orchestration, memory systems, RAG patterns. Notes-only (no `templates/`).

- `notes/` — distilled learnings + annotated reading list.
