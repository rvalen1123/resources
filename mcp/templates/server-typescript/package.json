{
  "name": "mcp-server-demo-ts",
  "version": "0.1.0",
  "description": "Minimal TypeScript MCP server starter (stdio transport, one tool).",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "mcp-server-demo-ts": "build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc && chmod +x build/index.js || true",
    "start": "node build/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
