# MCP

Model Context Protocol resources: server templates (TypeScript and Python), client config snippets, curated server list.

- `templates/` — server starters, client configs.
- `notes/` — transport types, curated servers, reading list.
