# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem
secrets/
credentials/

# Bootstrap state (per-machine, never shared)
bootstrap/.local-state/
*.bak-*

# Plan / scaffolding artifacts (author's working docs, not repo content)
docs/

# Local Claude Code project state (this repo is a library, not a CC-using project)
.claude/

