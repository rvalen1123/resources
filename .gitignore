# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem
secrets/
credentials/

# Bootstrap state (per-machine, never shared)
bootstrap/.local-state/
*.bak-*
